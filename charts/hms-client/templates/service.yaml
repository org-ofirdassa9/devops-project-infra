apiVersion: v1
kind: Service
metadata:
  name: {{ include "hms-client.fullname" . }}
  labels:
    {{- include "hms-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hms-client.selectorLabels" . | nindent 4 }}
