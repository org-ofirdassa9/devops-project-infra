apiVersion: v1
kind: Service
metadata:
  name: svc-hms-client
  labels:
    app: hms-client
spec:
  selector:
    app: hms-client
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
      